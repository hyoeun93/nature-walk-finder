function check(){
    let difficulty = document.quizForm.difficulty.value
    let surfaceType = document.quizForm.surfaceType.value
    let landform = document.quizForm.landform.value

    if (difficulty === "Difficulty Level : 1"){
        //somehow make this answer correspond to the API difficulty
 
    }

    if(difficulty ==="Difficulty Level : 2"){
          //somehow make this answer correspond to the API difficulty
        
    }

    if(surfaceType === "Dirt"){
        //somehow make this answer correspond to the API
  

    }

}